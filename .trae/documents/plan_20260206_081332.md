Here is the plan to complete the interview requirements:

# Interview Task Implementation Plan

## 1. Infrastructure Setup
- [ ] Create `docker-compose.yml` in the project root.
    - [ ] Configure **MySQL 8.0** service (database: `qa_db`, user: `user`, password: `password`).
    - [ ] Configure **phpMyAdmin** service for database management (port 8085).
- [ ] Start the database services using `docker-compose up -d`.

## 2. Backend Implementation (qa-service-user)
- [ ] **Dependencies**: Add `spring-boot-starter-data-jpa` and `mysql-connector-j` to [pom.xml](file:///home/jlguo/work/projects/trae_projects/qa-live-healthcare-interview/server/qa-service-user/pom.xml).
- [ ] **Configuration**: Update [application.properties](file:///home/jlguo/work/projects/trae_projects/qa-live-healthcare-interview/server/qa-service-user/src/main/resources/application.properties) with database connection details.
- [ ] **Data Model**: Create `Doctor` entity class matching the JSON structure.
- [ ] **Repository**: Create `DoctorRepository` interface.
- [ ] **Service**: Implement `DoctorService` to handle business logic.
- [ ] **Controller**: Create `DoctorController` to expose API endpoints (e.g., `GET /api/doctors`).
- [ ] **Data Migration**: Create a startup runner to load data from [doctor-user-list.json](file:///home/jlguo/work/projects/trae_projects/qa-live-healthcare-interview/web/qa-web/src/data/doctor-user-list.json) into the database if empty.

## 3. Frontend Implementation (web/qa-web)
- [ ] **Internationalization (i18n)**:
    - [ ] Install `vue-i18n`.
    - [ ] Create locale files: `src/locales/zh.json` and `src/locales/en.json`.
    - [ ] Configure i18n instance in `main.ts`.
    - [ ] Add a language switcher dropdown to the Home page header.
    - [ ] Replace hardcoded text on the Home page with i18n keys.
- [ ] **API Integration**:
    - [ ] Update [Doctors.vue](file:///home/jlguo/work/projects/trae_projects/qa-live-healthcare-interview/web/qa-web/src/views/Doctors.vue) to fetch data from the backend API instead of the local JSON file.
    - [ ] Handle loading and error states.

## 4. Verification & Documentation
- [ ] **Testing**: Create `curl` test scripts to verify the backend API.
- [ ] **Documentation**:
    - [ ] Create `API_TEST.md` documenting how to run the tests.
    - [ ] Create `/docs/api.md` describing the API endpoints.
